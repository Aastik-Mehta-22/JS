//Primitive data types are stored in the stack memory and the reference data types are stored in the heap memory.

///primitive data types

// 7 TYPES : string ,number ,boolean ,null ,undefined ,symbol ,bigint

//symbol
const id = Symbol('123')
const aid = Symbol('123')
console.log(id === aid);

const bignumber = 172982993829389n;//at last n shown ist bigint
//refrence (non primitive)
//array , object , funcitons

//-------STACK AND HEAP MEMORY ----//
//Primitive (stack) and non primitive (heap)
let myytname = "hcchodudhary"